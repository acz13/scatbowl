{
  "name": "scatbowl-shared",
  "private": true,
  "scripts": {
    "lint": "eslint --ignore-path .gitignore .",
    "lintfix": "eslint --fix --ignore-path .gitignore ."
  },
  "dependencies": {
    "htmlparser2": "^4.0.0",
    "node-fetch": "^2.6.0",
    "qs": "^6.8.0"
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "git add"
    ]
  }
}
