NODE_ENV=test # Disables postgres, set to anything else to enable it
POSTGRES_CONNECTION_STRING='postgresql://username:password@host:port/database'
REDIS_CONNECTION_STRING='redis://host:port/database' # for the same machine, usually use 'redis://localhost:6379/0'

PORT=3000 # the port the server itself listens on
IP=127.0.0.1 # which IP to bind to

# Unused so far
# FACEBOOK_CLIENT_ID=
# FACEBOOK_CLIENT_CLIENT=

# Obtained from google dashboard, only used when not testing
GOOGLE_CLIENT_ID=XXXXXXXXXXXX-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=THISISMYSECRET